volumes:
  - folder: AutoProcess
    mount_script: osascript -e 'mount volume "smb://username:password@hostname.tld/MySpecialSharename"'
    after_mount: docker compose -f /path/to/your/docker-compose.yml restart
  - folder: SomeOtherShare
    mount_script: some_other_script.sh
